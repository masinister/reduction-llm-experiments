services:
  dev:
    build: .
    volumes:
      - .:/workspace
      - ~/.cache/huggingface:/root/.cache/huggingface
    stdin_open: true
    tty: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]